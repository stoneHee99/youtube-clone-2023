extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="post")
        input(placeholder="Old Password", name="oldPassword")
        input(placeholder="New Password", name="newPassword")
        input(placeholder="New Password Confiemation", name="newPasswordConfirm")
        input(value="Change Password", type="submit")