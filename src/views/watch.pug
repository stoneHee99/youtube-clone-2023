extends base.pug

block content 
    div
        p=video.description
        small=video.createdAt 
    a(href=`${video.id}/edit`) Edit video 👉
    br
    a(href=`${video.id}/delete`) Delete video 👉